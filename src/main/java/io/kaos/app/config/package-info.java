/**
 * Owns the minimal local configuration needed by the KAOS application process.
 *
 * <p>This package does not own provider credentials, remote configuration, or
 * capability-specific settings. Those belong to the feature that introduces
 * the corresponding behavior.</p>
 */
package io.kaos.app.config;
