/**
 * Owns the first direct, local-only Ollama integration inside the single KAOS
 * application.
 *
 * <p>This package does not define model selection, prompt submission, response
 * streaming, a provider-neutral abstraction, or a separate runtime boundary.</p>
 */
package io.kaos.ai.ollama;
