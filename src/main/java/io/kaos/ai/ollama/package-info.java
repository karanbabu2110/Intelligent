/**
 * Owns the first direct, local-only Ollama integration inside the single KAOS
 * application.
 *
 * <p>This package owns local reachability and explicit model selection. It does
 * not define prompt submission, response streaming, a provider-neutral
 * abstraction, or a separate runtime boundary.</p>
 */
package io.kaos.ai.ollama;
