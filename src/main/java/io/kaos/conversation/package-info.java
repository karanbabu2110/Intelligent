/**
 * Owns the conversation vocabulary implemented inside the single KAOS application.
 *
 * <p>The package currently defines only immutable user and assistant messages. It does
 * not yet own a conversation aggregate, message history, persistence, selection, or
 * multi-turn AI request behavior.</p>
 */
package io.kaos.conversation;
