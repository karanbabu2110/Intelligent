/**
 * Owns one bounded agent workflow inside the single KAOS application.
 *
 * <p>The first implemented value is one immutable, non-persistent goal. It
 * preserves a bounded user objective but grants no tool or execution authority.
 * Planning, execution, state, approval coordination and results remain later
 * Epic 009 features.</p>
 */
package io.kaos.agent;
